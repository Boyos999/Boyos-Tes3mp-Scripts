# Fixed Levelup Attributes
1. Makes the attribute bonus on levelup fixed (x3 by default)
2. Put the script in server/scripts/custom, add the following to customScripts.lua
```lua
fixedLevelupAttributes = require("custom.fixedLevelupAttributes")
```